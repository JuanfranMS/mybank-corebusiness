package com.mybank.corebusiness.api.transaction;

/**
 * Different available types of channels, for transaction requests. 
 */
public enum Channels {
    CHANNEL_CLIENT,
    CHANNEL_ATM,
    CHANNEL_INTERNAL,
}